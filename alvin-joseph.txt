codepen link: https://codepen.io/aznmaniii/pen/yLVQMPv?editors=1100

    1. What is Semantic HTML? 
            HTML that introduces meaning to the web page rather than just presentation.
    2. What is HTML used for? 
            It is used to create electronic documents (called pages) that are displayed on the web.
    3. What is an attribute and where do we put it? 
            It provides additional information about HTML elements. You put them inside the start tag.
    4. What is the h1 tag used for? How many times should I use it on a page?
            Used to define the most important heading. Only once.
    5. Name two tags that have required attributes.
            <a href=""> and <img alt="">
    6. What do we put in the head of our HTML document? 
            <meta charset="utf-8">
            <title>Test</title>
    7. What is an id? 
            An attribute that specifies a unique id for an HTML element. 
    8. What elements can I add an id to? 
            any element
    9. How many times can I use the same id on a page? 
            only once
    10. What is a class? 
            An attribute used to specify a class for an HTML element. 
    11. What elements can I add a class to? 
            any element
    12. How many times can I use the same class on a page? 
            as many as needed
    13. How do I get my link to open in a new tab?
            <a href="link" target="_blank">
    14. What is the alt attribute in the image tag used for? 
            Provides an alternate text for an image if the user cannot view it. 
    15. How do I reference an id?
            # followed by id name
    16. What is the difference between a section and a div
            A section represents related sections of a document while a div is a generic container 
            that does not represent anything
    17. What is CSS used for? 
            Used to style an HTML document.
    18. How to we select an element? Example - every h2 on the page
            h2 {
                color:blue;
            }
    19. What is the difference between a class and an id? - Give me an example of when I might use each one
            Class is used for single or multiple elements on a page multiple timeswhile id is used for a 
            single element once. When you have alot of elements you need to change it is better to use class,
            when it is just a few you are better off using id. 
    20. How do we select classes in CSS?
            . followed by class name
    21. How do we select a p element with a single class of “human””?
            p .human {
                color:blue;
            }
    22. What is a parent child selector? When would this be useful?
            A selector that selects all elements that are a direct child of a specified element.
            Modifying a specific group of code in CSS 
    23. How do you select all links within a div with the class of sidebar?
            .sidebar a {
                color:blue
            }
    24. What is a pseudo selector?
            A selector that selects elements in a specific state.
    25. What do we use the change the spacing between lines?
            line-height:
    26. What do we use to change the spacing between letters?
            letter-spacing:
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
            text-transform: uppercase;,text-transform: lowercase;,text-transform: capitalize;
    28. How do I add a 1px border around my div that is dotted and black?
            div.test {
                border-style: dotted;
                border-color: black;
            }
    29. How do I select everything on the page? 
            * {
                color:blue;
            }
    30. How do I write a comment in CSS?
            /* comment here */
    31. How do I find out what file I am in, when I am using the command line?
            type pwd 
    32. Using the command line - how do I see a list of files/folders in my current folder?
            type ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? 
            rm filename / this is permanent 
    34. Why should I use version control? 
            To maintain a history of changes in case something needs to be corrected
    35. How often should I commit to github?
            Always after you make changes
    36. What is the command we would use to push our repo up to github? 
            git push -u origin 
    37. Walk me through Lambda's git flow. 
            Fork -> Clone -> Create a branch -> git add. (adds repo changes) ->
            git commit -m "" (saves changes w/ message) -> git push -u origin branch-name
            (push commits to github) -> Creat pull request in github
Stretch Questions

    1. What is the difference between an inline element and a block element?
            Block elements begin a new line on a web page and extends the full width of the
            available horizontal space. Inline elements can begin within a line and does not start a new line.
            They only extend as far as it is defined by tags. 
    2. What happens when an element is positioned absolutely? 
            It is positioned relative to its first positioned ancestor element.
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width?
            It will automatically take up the space it needs. You can change the width with the width: property
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that 
    is display inline-block by default
            headers and lists/ images and button/ span
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all 
    elements respect the width we've given them? 
            The box model is a box that wraps around every HTML element. It would be the HTML element. The box
            would move the elements out of the way.